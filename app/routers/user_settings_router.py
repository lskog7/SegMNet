# from fastapi import APIRouter, Depends
# from app.schemas.settings import SettingsUpdate
# from app.services.user_settings_service import update_settings

# router = APIRouter()

# @router.put("/update-settings")
# async def update_user_settings(settings: SettingsUpdate):
#     result = await update_settings(settings)
#     return result
